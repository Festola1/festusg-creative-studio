"use client";
import { useState } from "react";import { ArrowRight, X } from "lucide-react";import { projects } from "./festusg-data";
const cats=["All Projects","Websites","Flyers","Posters","Banners","AI Videos","Birthday Cards","Invitations","Social Media Designs"];
export function PortfolioGallery(){const [filter,setFilter]=useState("All Projects"),[active,setActive]=useState<(typeof projects)[number]|null>(null);const shown=filter==="All Projects"?projects:projects.filter(p=>p.category===filter),basePath=process.env.NEXT_PUBLIC_BASE_PATH??"",studioImage=`${basePath}/festusg-studio-green.jpg`;return <><div className="filters">{cats.map(c=><button key={c} onClick={()=>setFilter(c)} className={filter===c?"active":""}>{c}</button>)}</div><div className="project-grid">{shown.map((p,i)=><article className={`project-card ${i===0&&shown.length>3?"featured":""}`} key={p.title}><div className={`project-image ${p.tone}`} role="img" aria-label={`${p.title} creative project preview`} style={{backgroundImage:`url(${studioImage})`,backgroundPosition:p.pos}}><span>{p.category}</span><button onClick={()=>setActive(p)} aria-label={`View ${p.title}`}><ArrowRight/></button></div><div className="project-meta"><div><h3>{p.title}</h3><p>{p.desc}</p></div><button onClick={()=>setActive(p)}>View Project</button></div></article>)}</div>{active&&<div className="modal-backdrop"><div className="modal"><button className="modal-close" onClick={()=>setActive(null)} aria-label="Close project details"><X/></button><div className="modal-image" role="img" aria-label={active.title} style={{backgroundImage:`url(${studioImage})`,backgroundPosition:active.pos}}/><div className="modal-body"><div className="section-label">{active.category}</div><h2>{active.title}</h2><p>{active.desc}</p><div className="case-grid"><div><b>Client requirement</b><span>Create a distinctive visual that communicates clearly.</span></div><div><b>Creative approach</b><span>Bold hierarchy and a premium colour system tailored to the audience.</span></div><div><b>Final result</b><span>A polished, flexible design ready for promotional use.</span></div></div><a href={`${basePath}/contact/`} className="button">Start a Similar Project</a></div></div></div>}</>}
